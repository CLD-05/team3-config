apiVersion: v1
kind: Service
metadata:
  name: foldy-app
  labels:
    app: foldy
spec:
  type: ClusterIP
  selector:
    app: foldy
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
